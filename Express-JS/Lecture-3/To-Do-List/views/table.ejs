<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Users</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f9;
            color: #333;
        }

        h1 {
            text-align: center;
            margin-top: 40px;
            font-size: 2.5rem;
            color: #333;
            letter-spacing: 1px;
        }


        table {
            width: 90%;
            max-width: 1200px;
            margin: 30px auto;
            border-collapse: collapse;
            border-radius: 8px;
            background-color: #fff;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }

        th,
        td {
            padding: 15px;
            text-align: center;
            border: 1px solid #ddd;
            font-size: 1rem;
        }

        th {
            color: black;
            font-weight: 700;
            text-transform: uppercase;
        }

        tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        tr:hover {
            background-color: #f1f1f1;
        }


        a {
            color: #4CAF50;
            font-weight: bold;
            text-decoration: none;
            transition: color 0.3s ease, transform 0.2s ease;
        }

        a:hover {
            color: #4CAF50;
            transform: scale(1.05);
        }

        .add {
            text-align: center;
            margin-top: 30px;
        }

        .add a {
            display: inline-block;
            padding: 12px 25px;
            background-color: #4CAF50;
            color: white;
            font-size: 1.1rem;
            font-weight: bold;
            text-decoration: none;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

        .add a:hover {
            background-color: #4CAF50;
            transform: scale(1.05);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .add a:focus {
            outline: none;
        }


        .deletedata {
            padding: 6px 12px;
            background-color: #4CAF50;
            color: white;
            font-weight: bold;
            border-radius: 4px;
            display: inline-block;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

        .deletedata:hover {
            background-color: #4CAF50;
            color: #fff;
            transform: scale(1.05);
        }

        /* Responsive Styles */
        @media (max-width: 768px) {
            table {
                width: 95%;
                font-size: 0.9rem;
            }

            th,
            td {
                padding: 10px;
            }

            h1 {
                font-size: 2rem;
            }

            .add a {
                font-size: 1rem;
                padding: 10px 20px;
            }
        }

        
        @media (max-width: 480px) {
            h1 {
                font-size: 1.8rem;
            }

            table {
                width: 100%;
            }

            th,
            td {
                padding: 8px;
            }

            .add a {
                padding: 8px 15px;
                font-size: 0.9rem;
            }
        }
    </style>
</head>

<body>
    <h1>View Users</h1>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Email</th>
                <th>Phone</th>
                <th>Action</th>
                <th>Update</th>
            </tr>
        </thead>
        <tbody>
            <% record.map((val, i)=> { %>
                <tr>
                    <td>
                        <%= val.id %>
                    </td>
                    <td>
                        <%= val.name %>
                    </td>
                    <td>
                        <%= val.email %>
                    </td>
                    <td>
                        <%= val.phone %>
                    </td>
                    <td><a href="/deleteuser?deleteId=<%= val.id %>" class="deletedata">Delete</a></td>
                    <td><a href="/edituser?id=<%= val.id %>" class="deletedata">Edit</a></td>
                </tr>
                <% }) %>
        </tbody>
    </table>

    <div class="add">
        <a href="/form">Add User</a>
    </div>
</body>

</html>